package command

import "testing"

func TestCreateWallet_Handle(t *testing.T) {
	t.Run("cannot create walllet when empty currency-code", func(t *testing.T) {

	})

	t.Run("cannot create wallet when currency-code is invalid", func(t *testing.T) {

	})

	t.Run("cannot create wallet when repo save fail", func(t *testing.T) {

	})

	t.Run("can create wallet", func(t *testing.T) {

	})
}
